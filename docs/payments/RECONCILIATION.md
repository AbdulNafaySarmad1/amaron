# Reconciliation

Reconciliation queries the configured provider and records an immutable evaluation with matched, status mismatch, amount mismatch, or refund mismatch outcomes. Capture is not assumed to mean settlement; real provider adapters must supply settlement data before settlement reporting is enabled.
