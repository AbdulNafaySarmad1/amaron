import { OperationsPage } from "@/components/operations-page";
export default function Page() { return <OperationsPage config={{ title: "Demand", eyebrow: "PLANNING / DEMAND", description: "Inspect demand signals and request a forecast generation run.", endpoint: "/demand", empty: "No demand observations match the current filter.", chart: { label: "Demand series", fields: ["unitsOrdered", "orders", "revenue"] }, action: { path: "/forecasts/generate", label: "Generate forecast", selectedField: "variantId", body: { horizonDays: 30, inputWindowDays: 90, model: "MovingAverage" } } }} />; }
