import { Untranslated } from "@/components/i18n/untranslated";

export default function DesignSystemLayout({ children }: { children: React.ReactNode }) {
  return <Untranslated>{children}</Untranslated>;
}
